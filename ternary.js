// When we have only 2 conditions then we will use ternary operator.

// const number = 25;
// (number % 2 === 0) ? console.log('The number is even') : console.log('The number is odd');


const number = 20;
const oddEven = (number % 2 === 0) ? 'The number is even !' : 'The number is odd !';
console.log(oddEven);
